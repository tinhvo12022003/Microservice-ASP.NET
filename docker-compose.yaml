services:
  userservice:
    build:
      context: .
      dockerfile: UserService/Dockerfile

    container_name: userservice

    restart: always

    ports:
      - "3001:8080"

    environment:
      - ASPNETCORE_ENVIRONMENT=Production
